{
  "name": "sharedb-test-app",
  "version": "0.0.1",
  "description": "An app to test shareDB",
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p static/dist/ && ./node_modules/.bin/browserify client.js -o static/dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.14.0",
    "sharedb": "^1.0.0-beta",
    "websocket-json-stream": "^0.0.1",
    "ws": "^1.1.0",
    "jquery": "^3.1.1",
    "ot-text": "^1.0.1"
  },
  "devDependencies": {
    "browserify": "^13.0.1"
  }
}
